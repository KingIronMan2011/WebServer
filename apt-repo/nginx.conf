server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    # Browse repository paths in a regular browser, similar to Debian mirrors.
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    location / {
        try_files $uri =404;
    }
}
